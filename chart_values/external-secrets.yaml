---
name: sirena

deployment:
  enable: false
ingress:
  enable: false
service:
  enable: false

externalSecret:
  database:
    enable: true
    databaseName: sirena-formation
  redis:
    enable: true
    clusterName: sirena-formation-redis
  bucket:
    enable: true
    bucketName: sirena-formation-s3
  registry:
    enable: true
  app:
    enable: true
    targetSecretName: backend
    template:
      AUTH_TOKEN_SECRET_KEY: "{{ .AUTH_TOKEN_SECRET_KEY }}"
      DEMAT_SOCIAL_API_TOKEN: "{{ .DEMAT_SOCIAL_API_TOKEN }}"
      PC_CLIENT_ID: "{{ .PC_CLIENT_ID }}"
      PC_CLIENT_SECRET: "{{ .PC_CLIENT_SECRET }}"
      REFRESH_TOKEN_SECRET_KEY: "{{ .REFRESH_TOKEN_SECRET_KEY }}"
      SARBACANE_API_KEY: "{{ .SARBACANE_API_KEY }}"
      SENTRY_DSN_BACKEND: "{{ .SENTRY_DSN_BACKEND }}"
      SUPER_ADMIN_LIST_EMAIL: "{{ .SUPER_ADMIN_LIST_EMAIL }}"
